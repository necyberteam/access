<?php

/**
 * @file
 * Module for CSSN.
 */

use Drupal\Core\Access\AccessResult;
use Drupal\Core\Entity\EntityInterface;
use Drupal\Core\Form\FormStateInterface;
use Drupal\Core\Render\Markup;
use Drupal\Core\Session\AccountInterface;
use Drupal\block\Entity\Block;
use Drupal\cssn\Plugin\Util\RoleProgramSorter;
use Drupal\taxonomy\Entity\Term;
use Drupal\user\Entity\User;
use Drupal\views\ViewExecutable;

/**
 * Implements hook_views_pre_render().
 */
function cssn_views_pre_render(ViewExecutable $view): void {
  if ($view->id() == 'cssn_directory') {
    $view->element['#attached']['library'][] = 'cssn/cssn_directory';
  }
}

/**
 * Implements hook_preprocess_HOOK() for views-view-field.html.twig.
 *
 * @phpstan-param array<string, mixed> $variables
 */
function cssn_preprocess_views_view_field(array &$variables): void {
  $view = $variables['view'];
  $field = $variables['field'];

  // Overwrite cssn directory on /community/cssn/directory.
  if ($view->id() == 'cssn_directory' && $view->current_display == 'page_1' && $field->options['id'] == 'nothing') {
    $output = $variables['output']->__toString();
    $lines = explode("\n", $output);

    $new_output = '';
    $badges = "<ul class='list-none'>";
    foreach ($lines as $key => $line) {
      if ($key == 0) {
        $term_ids = explode(', ', $line);
        if (!empty($term_ids[0])) {
          foreach ($term_ids as $term_id) {
            $term = Term::load($term_id);
            /** @var \Drupal\file\FileInterface|null $badge_file */
            $badge_file = $term ? $term->get('field_badge')->entity : NULL;
            if ($badge_file != NULL) {
              $name = $term->get('name')->value;
              $description = $term->get('description')->value;
              $description = $description ? strip_tags($description) : '';
              $badge_value = $term->get('field_badge')->getValue();
              $image_alt = $badge_value[0]['alt'] ?? '';
              $image_url = $badge_file->getFileUri();
              $image = \Drupal::service('file_url_generator')->generateAbsoluteString($image_url);
              if ($image) {
                if ($name) {
                  $badges .= "<li class='me-2 mb-1 position-relative inline-block d-inline-block' data-placement='left' data-toggle='tooltip' title='$name'>";
                }
                else {
                  $badges .= "<li class='ms-2 mb-1'>";
                }
                $badges .= "<img src='$image' alt='$image_alt' title='$name' width='35' height='35' />";
                $badges .= "</li>";
              }
            }
          }
          $badges .= "</ul>";
        }
        else {
          $badges = '';
        }
      }
      elseif (
        $line == "!!!!=-=-= This line is programmatically replaced, if you change anything in this line you'll break badges. See cssn.module to fix any issues =-=-=-=-!!!!") {
        $new_output .= $badges;
      }
      else {
        $new_output .= $line;
      }
    }
    $variables['output'] = Markup::create($new_output);
  }
}

/**
 * Implements hook_block_access().
 *
 * Show welcometothecssn block to people in the ACCESS CSSN program/region.
 * Show jointhecssn block to other people.
 */
function cssn_block_access(Block $block, string $operation, AccountInterface $account): AccessResult {
  if ($block->getPluginId() == 'block_content:58e0d082-2224-43c2-a911-c41e5afc47b9') {
    // If the User is part of the ACCESS CSSN program, show the welcome block.
    $current_user = \Drupal::currentUser();
    if ($current_user->isAnonymous()) {
      return AccessResult::forbidden();
    }
    $user = User::load($current_user->id());
    $regions = $user->get('field_region')->getValue();
    foreach ($regions as $region) {
      // ACCESS CSSN term id = 780.
      if ($region['target_id'] == '780') {
        return AccessResult::allowed();
      }
    }
    return AccessResult::forbidden();
  }
  // Show jointhecssn block to other people.
  if ($block->getPluginId() == 'block_content:eedf2b47-d6e0-40d9-9a83-1de706df2843') {
    $current_user = \Drupal::currentUser();
    if ($current_user->isAnonymous()) {
      return AccessResult::allowed();
    }
    $user = User::load($current_user->id());
    $regions = $user->get('field_region')->getValue();
    foreach ($regions as $region) {
      // ACCESS CSSN term id = 780.
      if ($region['target_id'] == '780') {
        return AccessResult::forbidden();
      }
    }
    return AccessResult::allowed();
  }
  return AccessResult::neutral();
}

/**
 * Return a list of selectable roles for CSSN form.
 *
 * @return string[]
 *   The role ids that the CSSN forms expose.
 */
function cssn_get_roles_array(): array {
  return [
    "mentor",
    "student",
    "research_computing_facilitator",
    "research_software_engineer",
    "ci_systems_engineer",
    "researcher",
  ];
}

/**
 * Implements hook_form_alter().
 *
 * @phpstan-param array<string, mixed> $form
 */
function cssn_form_alter(array &$form, FormStateInterface $form_state, string $form_id): void {
  // Handle role forms.
  if (
    $form_id == 'webform_submission_edit_your_cssn_roles_add_form' ||
    $form_id == 'webform_submission_edit_your_cssn_roles_edit_form'
  ) {
    // Set submit action.
    $form['actions']['submit']['#submit'][] = 'cssn_role_form_submit';

    // Update form's check marks for roles in user's profile.
    $user = \Drupal::currentUser();
    $user_roles = array_intersect($user->getRoles(), cssn_get_roles_array());
    $form['elements']['roles_']['#default_value'] = $user_roles;

    // Update form's academic status.
    cssn_form_update_academic_status($form);
  }

  // Handle join cssn forms.
  if (
    $form_id == 'webform_submission_join_the_cssn_network_node_6111_add_form' ||
    $form_id == 'webform_submission_join_the_cssn_network_node_6111_edit_form'
  ) {
    // Update form's check marks for roles in user's profile.
    $fields_to_roles = [
      'cssn' => 'General Member',
      'mentor' => 'MATCH Mentor',
      'student' => 'Student-Facilitator',
      'consultant' => 'MATCH Consultant',
      'cip' => 'NSF-funded CIP',
    ];
    $user = \Drupal::currentUser();
    $roles = $user->getRoles();
    $roles_set = [];
    foreach ($fields_to_roles as $field => $role) {
      if (in_array($field, $roles)) {
        $roles_set[] = $role;
      }
    }

    $form['elements']['i_am_joining_as_a_']['#default_value'] = $roles_set;

    $user_loaded = \Drupal::entityTypeManager()->getStorage('user')->load($user->id());
    // Get user's field_region.
    $regions = $user_loaded->get('field_region')->getValue();
    $cssn_member = 0;
    foreach ($regions as $region) {
      if ($region['target_id'] == '780') {
        $cssn_member = 1;
        break;
      }
    }

    $form['elements']['cssn_member']['#default_value'] = $cssn_member;

    /*
     * Handle case where user has roles but no "ACCESS CSSN" region.
     * This could happen if the user selects a CSSN role through
     * the edit_cssn_roles form.
     */
    // Lookup taxonomy term id by name 'ACCESS CSSN'.
    $term = \Drupal::entityTypeManager()->getStorage('taxonomy_term')->loadByProperties(['name' => 'ACCESS CSSN']);
    $term = reset($term);
    $term_id = $term->id();
    $role_program_sorter = new RoleProgramSorter($user, \Drupal::entityTypeManager(), \Drupal::messenger());
    $region_set = $role_program_sorter->lookupRegion($term_id);
    if ($form_id == 'webform_submission_join_the_cssn_network_add_form' && $roles_set && !$region_set) {
      \Drupal::messenger()->addMessage(t("The boxes checked below reflect your current roles, please select 'General Member' and submit this form to participate in the CSSN."), 'warning');
    }

    // Set submit action.
    $form['actions']['submit']['#submit'][] = 'cssn_form_submit';

    // Update form's academic status.
    cssn_form_update_academic_status($form);
  }
}

/**
 * Initialize form element academic status.
 *
 * @param array<string, mixed> $form
 *   The form structure.
 */
function cssn_form_update_academic_status(array &$form): void {
  $user = \Drupal::currentUser();
  $account = User::load($user->id());
  $academic_status = $account->get('field_academic_status')->getValue();
  $academic_status = count($academic_status) ? $academic_status[0]['value'] : NULL;
  $form['elements']['academic_status_select2']['#default_value'] = $academic_status;
}

/**
 * Form submit handler for the CSSN roles webform.
 *
 * @param array<string, mixed> $form
 *   The form structure.
 * @param \Drupal\Core\Form\FormStateInterface $form_state
 *   The form state.
 */
function cssn_role_form_submit(array &$form, FormStateInterface $form_state): void {
  $form_roles = cssn_get_roles_array();
  $user = \Drupal::currentUser();
  $role_program_sorter = new RoleProgramSorter($user, \Drupal::entityTypeManager(), \Drupal::messenger());
  foreach ($form_roles as $role) {
    $set_role = in_array($role, $form['elements']['roles_']['#value']);
    if ($set_role) {
      $role_program_sorter->addRole($role);
    }
    else {
      $role_program_sorter->removeRole($role);
    }
  }

  // If 'student' is selected, update academic status.
  if (in_array("student", $form['elements']['roles_']['#value'])) {
    cssn_update_academic_status($user, $form);
  }
}

/**
 * Form submit handler for the join the CSSN network webform.
 *
 * @param array<string, mixed> $form
 *   The form structure.
 * @param \Drupal\Core\Form\FormStateInterface $form_state
 *   The form state.
 */
function cssn_form_submit(array &$form, FormStateInterface $form_state): void {
  $elements = $form['elements']['i_am_joining_as_a_'];

  $general = $elements['General Member']['#checked'] ?? FALSE;
  $consultant = $elements['MATCH Consultant']['#checked'] ?? FALSE;
  $cip = $elements['NSF-funded CIP']['#checked'] ?? FALSE;
  // Lookup taxonomy term id by name 'ACCESS CSSN'.
  $term = \Drupal::entityTypeManager()->getStorage('taxonomy_term')->loadByProperties(['name' => 'ACCESS CSSN']);
  $term = reset($term);
  $term_id = $term->id();
  $enroll_program = FALSE;
  if ($general || $consultant || $cip) {
    $enroll_program = TRUE;
  }
  $user = \Drupal::currentUser();
  cssn_role_region('cssn', $general, $term_id, $enroll_program, $user);
  cssn_role_region('consultant', $consultant, $term_id, $enroll_program, $user);
  cssn_role_region('cip', $cip, $term_id, $enroll_program, $user);
}

/**
 * Update user's academic status.
 *
 * @param \Drupal\Core\Session\AccountInterface $user
 *   The account to update.
 * @param array<string, mixed> $form
 *   The submitted form structure.
 */
function cssn_update_academic_status(AccountInterface $user, array $form): void {
  $academic_status = $form['elements']['academic_status_select2']['#value'];
  $account = User::load($user->id());
  $account->get('field_academic_status')->setValue($academic_status);
  $account->save();
}

/**
 * Set or remove role and region and add badge for CSSN.
 *
 * @param string $role
 *   The role id to add or remove.
 * @param bool $checked
 *   Whether the role was selected on the form.
 * @param int|string $term_id
 *   The taxonomy term id of the CSSN region.
 * @param bool $enroll_program
 *   Whether the user should be enrolled in the CSSN program.
 * @param \Drupal\Core\Session\AccountInterface $user
 *   The account to update.
 */
function cssn_role_region(string $role, bool $checked, $term_id, bool $enroll_program, AccountInterface $user): void {
  $role_program_sorter = new RoleProgramSorter($user, \Drupal::entityTypeManager(), \Drupal::messenger());
  $scipe_badge = \Drupal::service('access_badges.badgeTools')->getBadgeTid('SCIPE Awardee');

  // Lookup user field 'field_user_badges'.
  $account = User::load($user->id());
  $badgetid = $account->get('field_user_badges')->getValue();

  if ($checked) {
    if (!empty($role)) {
      $role_program_sorter->addRole($role);
      if ($role == 'cip') {
        $has_badge = array_filter($badgetid, fn($b) => $b['target_id'] == $scipe_badge);
        if (empty($has_badge)) {
          $badgetid[] = ['target_id' => $scipe_badge];
          $account->set('field_user_badges', $badgetid);
          $account->save();
        }
      }
      // Flag user as part of the CSSN Affinity Group.
      $cssn_affinity_group = \Drupal::entityTypeManager()
        ->getStorage('taxonomy_term')
        ->loadByProperties(['tid' => 794]);
      $cssn_affinity_group = reset($cssn_affinity_group);

      if ($cssn_affinity_group) {
        $flag_id = 'affinity_group';
        $flag = \Drupal::service('cssn.flag');
        $set_flag = $flag->setFlag($flag_id, $cssn_affinity_group, $account);
        if ($set_flag) {
          \Drupal::messenger()->addMessage(t('You have been added to the CSSN Affinity Group.'));
        }
      }
      else {
        \Drupal::logger('cssn')->error('Could not load CSSN affinity group with tid 794');
      }
    }
    if ($enroll_program) {
      $role_program_sorter->addFieldRegion($term_id);
    }
  }
  else {
    if (!empty($role)) {
      $role_program_sorter->removeRole($role);
      if ($role == 'cip') {
        $badge_unset = FALSE;
        foreach ($badgetid as $key => $badge) {
          if ($badge['target_id'] == $scipe_badge) {
            unset($badgetid[$key]);
            $badge_unset = TRUE;
          }
        }
        if ($badge_unset) {
          $account->set('field_user_badges', $badgetid);
          $account->save();
        }
      }
    }
    if ($enroll_program === FALSE) {
      $role_program_sorter->removeFieldRegion($term_id);
    }
  }
}

/**
 * Implements hook_entity_update().
 *
 * Invalidates community persona caching when a user is updated.
 */
function cssn_entity_update(EntityInterface $entity): void {
  $type = $entity->bundle();
  if ($type == 'user') {
    \Drupal::service('cache_tags.invalidator')->invalidateTags(['community_persona']);
  }
}

/**
 * Implements hook_page_attachments_alter().
 *
 * @phpstan-param array<string, mixed> $attachments
 */
function cssn_page_attachments_alter(array &$attachments): void {
  $route_match = \Drupal::routeMatch();

  if ($route_match->getRouteName() === 'cssn.public_page') {
    $uid = $route_match->getParameter('uid');

    if (is_numeric($uid)) {
      $user = User::load($uid);

      if ($user && count($user->get('field_region')->getValue()) > 0 &&
          !($user->hasField('field_hide_community_profile') &&
            (bool) $user->get('field_hide_community_profile')->value === TRUE)) {
        $first_name = $user->get('field_user_first_name')->value ?? '';
        $last_name = $user->get('field_user_last_name')->value ?? '';
        $full_name = trim("$first_name $last_name");

        // Get current URL.
        $request = \Drupal::request();
        $current_url = $request->getSchemeAndHttpHost() . $request->getRequestUri();

        // Get site name from config.
        $site_config = \Drupal::config('system.site');
        $site_name = $site_config->get('name') ?? 'ACCESS';

        // Get user bio for description.
        $bio = $user->get('field_user_bio')->value ?? '';
        $description = !empty($bio) ? strip_tags($bio)
          : "View $full_name's community persona profile on $site_name.";
        // Trim description to 160 characters, a meta description best
        // practice.
        if (strlen($description) > 160) {
          $description = substr($description, 0, 157) . '...';
        }

        $title = "$full_name | Community Persona";

        // Remove metatag module's global defaults that conflict with the
        // persona-specific tags we're about to add. The metatag module fires
        // in hook_page_attachments() before this _alter hook runs.
        $metatag_keys_to_remove = [
          'description',
          'canonical_url',
          'image_src',
          'og_title',
          'og_description',
          'og_type',
          'og_url',
          'og_site_name',
          'og_image_url',
        ];
        if (!empty($attachments['#attached']['html_head'])) {
          $attachments['#attached']['html_head'] = array_values(
            array_filter($attachments['#attached']['html_head'], function ($item) use ($metatag_keys_to_remove) {
              return !isset($item[1]) || !in_array($item[1], $metatag_keys_to_remove);
            })
          );
        }

        // Standard meta tags.
        $meta_tags = [
          'description' => $description,
        ];

        // Open Graph tags.
        $og_tags = [
          'og:site_name' => $site_name,
          'og:type' => 'profile',
          'og:url' => $current_url,
          'og:title' => $title,
          'og:description' => $description,
        ];

        // Optional: Add user image if available.
        if ($user->hasField('user_picture') && !$user->get('user_picture')->isEmpty()) {
          /** @var \Drupal\file\FileInterface|null $user_picture */
          $user_picture = $user->get('user_picture')->entity;
          if ($user_picture) {
            $image_uri = $user_picture->getFileUri();
            $image_url = \Drupal::service('file_url_generator')->generateAbsoluteString($image_uri);
            $og_tags['og:image'] = $image_url;
            $meta_tags['image'] = $image_url;
          }
        }

        // Add standard meta tags.
        foreach ($meta_tags as $key => $value) {
          $attachments['#attached']['html_head'][] = [
            [
              '#tag' => 'meta',
              '#attributes' => [
                'name' => $key,
                'content' => $value,
              ],
            ],
            'cssn_meta_' . $key,
          ];
        }

        // Add Open Graph tags.
        foreach ($og_tags as $property => $content) {
          $attachments['#attached']['html_head'][] = [
            [
              '#tag' => 'meta',
              '#attributes' => [
                'property' => $property,
                'content' => $content,
              ],
            ],
            'cssn_' . str_replace(':', '_', $property),
          ];
        }

        // Add canonical URL.
        $attachments['#attached']['html_head'][] = [
          [
            '#tag' => 'link',
            '#attributes' => [
              'rel' => 'canonical',
              'href' => $current_url,
            ],
          ],
          'cssn_canonical_url',
        ];
      }
    }
  }
}
